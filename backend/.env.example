# Database Configuration
# For local development with SQLite (default)
DATABASE_URL=sqlite:///./dialed_in.db

# For PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@localhost:5432/dialed_in_db

# API Configuration
API_HOST=localhost
API_PORT=8000
